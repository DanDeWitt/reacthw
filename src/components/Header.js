import React from "react";

const Header = () => {
    return ( <
        header >
        <
        h1 > Course App < /h1> <
        p > Add Course item(s) using the input box below < /p> < /
        header >
    )
}

export default Header